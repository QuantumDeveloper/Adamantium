<View xmlns="http://adamantium/ui"
      xmlns:x="http://adamantium/ui/xaml/extensions">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
    <StackPanel Orientation="Vertical" Margin="24">

        <TextBlock Text="Color Picker"
                   FontSize="20"
                   MarginBottom="4"/>
        <TextBlock Text="An inline HSV picker: drag the square (saturation / value), the hue bar and the alpha bar, or type a hex / R-G-B-A value. The picked colour is fed into the THEME accent, so the controls on the right recolour live."
                   FontSize="13"
                   Foreground="{ResourceReference TextFillColorSecondary}"
                   MaxWidth="680"
                   HorizontalAlignment="Left"
                   MarginBottom="18"/>

        <StackPanel Orientation="Horizontal">
            <ColorPicker SelectedColor="{Binding PickedColor, Mode=TwoWay}"
                         VerticalAlignment="Top"/>

            <StackPanel Orientation="Vertical"
                        MarginLeft="32"
                        VerticalAlignment="Top">
                <TextBlock Text="Colour wheel (conic hue × radial saturation)"
                           FontSize="14"
                           MarginBottom="14"/>
                <!-- Two-way to the SAME PickedColor as the inline picker. The wheel picks hue+saturation; its OWN Value slider
                     below picks brightness (so the wheel alone is a full picker and can reach black at 0). All stay in sync
                     and drive the theme accent. The wheel dims toward the value. -->
                <ColorWheel SelectedColor="{Binding PickedColor, Mode=TwoWay}"
                            Value="{Binding WheelValue, Mode=TwoWay}"/>
                <TextBlock Text="Value (brightness — 0 = black)" FontSize="12" MarginTop="12" MarginBottom="2"
                           Foreground="{ResourceReference TextFillColorSecondary}"/>
                <Slider Width="256" Minimum="0" Maximum="1" Value="{Binding WheelValue, Mode=TwoWay}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical"
                        MarginLeft="32"
                        VerticalAlignment="Top">
                <TextBlock Text="Compact swatch → flyout (ColorPickerButton)"
                           FontSize="14"
                           MarginBottom="14"/>
                <!-- Same PickedColor as the inline picker + wheel: the swatch shows it; clicking opens a floating,
                     light-dismissing ColorPicker flyout - editing there stays in sync and drives the theme accent too. -->
                <ColorPickerButton SelectedColor="{Binding PickedColor, Mode=TwoWay}"
                                   Width="180" Height="30"
                                   HorizontalAlignment="Left"/>
            </StackPanel>

            <StackPanel Orientation="Vertical"
                        MarginLeft="32"
                        VerticalAlignment="Top">
                <TextBlock Text="Live theme accent"
                           FontSize="14"
                           MarginBottom="14"/>
                <CheckBox Content="Checkbox"
                          IsChecked="True"
                          MarginBottom="12"/>
                <RadioButton Content="Radio button"
                             IsChecked="True"
                             MarginBottom="12"/>
                <ToggleSwitch IsChecked="True"
                              MarginBottom="16"/>
                <Slider Width="200"
                        Value="60"
                        MarginBottom="16"/>
                <ProgressBar Width="200"
                             Value="60"/>
            </StackPanel>
        </StackPanel>

    </StackPanel>
    </ScrollViewer>
</View>
