namespace Adamantium.Fonts.OTF
{
    public static class BezierSampler
    {
        public static Outline[] SampleOutlines(Outline[] outlines, int rate)
        {
            var sampledOutlines = new Outline[outlines.Length];
            foreach (var outline in outlines)
            {
                
            }

            return sampledOutlines;
        }
    }
}