# Докинг — дизайн и дорожная карта

Цель: панели, которые можно таскать, стыковать, расщеплять и отрывать в отдельные окна, с сохраняемой
раскладкой. Это фундамент редакторского UI (инспектор, иерархия, консоль, вьюпорт) — то есть та вещь,
без которой движок не выглядит как среда разработки.

Ниже — дизайн и порядок работ, а в конце **свод правил поведения**: как панели обязаны себя вести в каждом
состоянии, по пунктам, на которые можно ссылаться из тестов.

---

## Главный вывод, из которого растёт вся конструкция

**Целиться курсором в то, что закрыто перетаскиваемым окном, нельзя.**

Это не рассуждение, а результат попытки: стыковка «наведи оторванное окно на полосу вкладок и отпусти»
была реализована, дошла до рабочего состояния — и оказалась негодной. Окно физически закрывает ту самую
полосу, в которую целишься; чем точнее хочешь попасть, тем меньше видно. Никакая точность хит-теста этого
не лечит. Код удалён (см. коммит `877c021`), и повторять этот заход не нужно.

Отсюда два следствия, определяющих архитектуру:

1. **Цель рисуется ПОВЕРХ перетаскиваемого окна** — компас с индикаторами сторон, а не хит-тест по скрытой
   панели. Пользователь целится в индикатор, и он же показывает, что именно произойдёт: вкладкой в группу
   или отдельной областью слева/справа/сверху/снизу. Без компаса эти варианты вообще неразличимы.
2. **Нужен настоящий layout-контрол**, а не «положить в существующую панель». Стыковка «влево» обязана
   куда-то класть — значит нужно дерево областей с разделителями, умеющее расщепляться и схлопываться.

---

## Второй вывод: чем НЕ должен быть этот контрол

Готовые докинг-контролы (тот же телериковский) регулярно оказываются непрозрачными: непонятно, как ими
пользоваться, что можно поменять, а нужного часто нельзя вовсе — и приходится хачить внутренности, после
чего появляются случайные вылеты и подглючивания. Это не невезение, у такого исхода есть конкретные
причины, и их видно заранее:

- **Модель срослась с визуалом.** Раскладка живёт внутри контролов, поэтому изменить её можно только
  мышью — не кодом и не тестом. Отсюда же и «поменять нельзя, только хакнуть».
- **Жесты идут мимо операций.** Перетаскивание делает одно, а публичный API — другое, и они расходятся.
- **Контрол решает за приложение.** Нет способа сказать «сюда нельзя» или «эту панель не закрывать» —
  остаётся перехватывать внутренние события и драться с контролом.
- **Скрытое глобальное состояние.** Статические реестры окон и «текущего перетаскивания» — прямой путь к
  случайным падениям, потому что состояние переживает то, что его породило.
- **Раскладка сохраняется непрозрачным блобом**, который нельзя ни прочитать, ни поправить руками.

Поэтому здесь, по пунктам, наоборот:

1. **Модель — открытые сериализуемые ДАННЫЕ.** Любую раскладку можно собрать кодом, а не только мышью.
   Раскладка, которую нельзя построить в тесте, не может быть протестирована — и не будет.
2. **Одна дорога.** Жест не делает ничего своего: он вызывает те же операции модели, что и код. Второго
   пути не существует, поэтому им нечем разойтись.
3. **Контрол спрашивает, а не решает.** Отменяемые события/политики перед стыковкой, закрытием и отрывом:
   приложение говорит «нельзя», не залезая внутрь.
4. **Сохранённая раскладка — читаемый текст** с версией, который можно посмотреть глазами и поправить.
5. **Никакого скрытого глобального состояния.** Хост владеет своим деревом; два хоста друг о друге не
   знают. Всё, что живёт дольше жеста, принадлежит кому-то явно.

## Что уже есть (на это опираемся)

- **Отрыв вкладки в окно** — `TabControl.TabTornOff` + `TabTearOffEventArgs`; жест распознаётся по
  ПЕРПЕНДИКУЛЯРНОМУ уходу от полосы, поэтому перестановка вдоль полосы его не будит. Окно создаёт
  приложение, не контрол.
- **Перенос окна нативным циклом** — `WindowBase.DragMove()` (Win32 `ReleaseCapture` + `WM_NCLBUTTONDOWN`),
  с Aero Snap и прилипанием к краям.
- **Видимость нативного перетаскивания** — `WindowBase.WindowMoving` / `WindowMoveCompleted` (из
  `WM_MOVING` / `WM_EXITSIZEMOVE`). Во время цикла ОС забирает мышь, и это ЕДИНСТВЕННЫЙ доступный взгляд
  на жест. Пока не используется никем — заведено ровно под докинг.
- **Позиция окна** — `Window.Left`/`Top` действительно двигают окно; `Window.TitleBarHeight` — высота
  каптиона, по которой тема меряет заголовок.
- **Прозрачное topmost click-through окно** — `IDragGhost` (layered + `UpdateLayeredWindow`), уже носит
  призрак драг-дропа. Компас — тот же приём.
- **Дыра в раскладке с карточкой-плейсхолдером** — `VirtualizingPanel.DropGapIndex` +
  `ItemsControl.DropPlaceholderTemplate` + движение между ответами раскладки. Готовый ответ на вопрос
  «куда встанет вкладка», когда стыкуемся ИМЕННО в полосу.
- **Кросс-оконный hit-test** — `IApplicationPlatform.WindowFromScreenPoint` (спрашиваем у ОС, а не
  перебираем свои окна) из драг-дропа.

---

## Иерархия и имена

Две параллельные иерархии — ДАННЫЕ и ВИЗУАЛ, — и они намеренно разные типы: визуал строится по данным и
никогда не является их хранилищем (см. «модель срослась с визуалом» выше).

**Правило именования, чтобы не гадать при добавлении нового типа:** `Docking*` — это КОРНИ и система целиком
(`DockingArea` показывает главный корень, `DockingWindow` — плавающий, `DockingLayout` — их лес), а `Pane*` —
то, что живёт ВНУТРИ корня (`PaneHost`, `PaneGroup`, `PaneSplitter`, `Pane`). Поэтому окно плавающей панели —
`DockingWindow`, а не `PaneWindow`: оно родственник `DockingArea`, а не `PaneGroup`, и содержит внутри себя те
же `PaneHost`/`PaneGroup`. Единственное исключение — `DockCompass`: он не корень, но и не содержимое, а
инструмент жеста, и слово «Dock» там про стыковку.

### Данные (`Adamantium.UI.Controls.Docking`, чистые сериализуемые типы)

| Тип | Что это |
|---|---|
| `DockingLayout` | Вся раскладка: список корней + версия формата. Корень сериализации. |
| `DockingRoot` | Один корень: ЭКРАННЫЙ прямоугольник + дерево узлов. Главное окно и каждое плавающее — равноправные корни. |
| `PaneNode` | Абстрактный узел. Ровно два наследника, третьего не заводить. |
| `PaneSplitNode : PaneNode` | Ориентация + дети. Свою длину каждый ребёнок несёт сам. Делится дальше на такие же узлы — рекурсия здесь и живёт. |
| `PaneGroupNode : PaneNode` | Лист: список ИДЕНТИФИКАТОРОВ пейнов + активный. Это и есть «группа вкладок». |
| `DockZone` | Перечисление: `Center`, `Left`, `Right`, `Top`, `Bottom`, `Floating`. Нейтральные данные — вьюмодель вправе им пользоваться. |
| `PanePolicy` | Сериализуемый слепок политики пейна (`Zone`, `Allowed`, `MinSize`, `Restore`) — то, что хранится, когда живого `Pane` ещё нет. |

Группа ссылается на пейны **по идентификатору**, а не по объекту: иначе модель перестаёт быть данными и
её нечем сериализовать.

### Визуал (контролы)

| Контрол | Роль | Чему соответствует |
|---|---|---|
| `DockingArea` | Корневой контрол: хостит пейны, владеет `DockingLayout`, отдаёт `ItemsSource`. | `DockingLayout` |
| `PaneHost` | СВОЯ панель: фиксированные дети берут свои пиксели, остаток делят звёзды; между ними разделители. | `PaneSplitNode` |
| `PaneGroup` | Область с вкладками — несколько `Pane` в одном месте. | `PaneGroupNode` |
| `Pane` | ЕДИНИЦА докинга: заголовок + содержимое + политика (`Zone`, `Allowed`, `MinSize`, `Restore`). | запись в `PaneGroupNode` |
| `PaneSplitter` | Разделитель между детьми сплита; драг фиксирует ПИКСЕЛИ обоих соседей, и они доезжают В МОДЕЛЬ. Визуал и жест переиспользуем у `GridSplitter`/`Thumb`. | длины в `PaneSplitNode` |
| `DockCompass` | Индикаторы зон и полупрозрачный предпросмотр. Живёт в ОДНОМ прозрачном topmost click-through окне на всю область (`DockCompassWindow`), которое поднимается на время жеста и не двигается. | — (живёт только на время жеста) |
| `DockingWindow` | Окно плавающего корня: внутри такой же `PaneHost`/`PaneGroup`. | `DockingRoot` (не главный) |

### Как это выглядит в разметке

Автор пишет ГДЕ, а не сколько:

```xml
<DockingArea>
    <PaneGroup Zone="Center">
        <Pane Header="Scene" Allowed="Center,Floating"/>
        <Pane Header="Game"  Allowed="Center,Floating"/>
    </PaneGroup>

    <PaneGroup Zone="Right"  Size="220"><Pane Header="Inspector" MinSize="180"/></PaneGroup>
    <PaneGroup Zone="Bottom" Size="180"><Pane Header="Console"/></PaneGroup>
</DockingArea>
```

**Долей в разметке нет — и это принципиально.** `Fraction="0.75"` отвечает на вопрос «сколько», когда автор
задаёт «где», и по нему невозможно понять, куда попадут вкладки: доля от какого сплита, считая какие
уровни. Хуже того, это величина ВРЕМЕНИ ВЫПОЛНЕНИЯ — её меняет пользователь, таща разделитель, и она
уезжает в сохранённую раскладку; написанное в разметке число перестаёт быть правдой после первого же
перетаскивания. Поэтому автор называет ЗОНУ, а дерево сплитов `DockingArea` строит из зон сама.
`Size` — необязательное число в ПИКСЕЛЯХ вдоль оси своей зоны: «инспектор начинается 220 в ширину».

### Размер узла: ОДНО число с режимом

У каждого узла ровно одна величина — `PaneLength`, и она либо **пиксели**, либо **вес в остатке** (звезда,
как у `Grid`). Раздача тоже как у `Grid`: фиксированные снимают своё сверху, остаток делится между
звёздами по весам.

```csharp
PaneLength.Pixels(240)   // 240 вдоль оси ряда
PaneLength.Star          // «беру, что останется»
PaneLength.Stars(0.25)   // четверть остатка против других звёзд
```

**Раньше чисел было два** — доля И пиксельная подсказка, — и это была ошибка конструкции, а не реализации.
Два числа на одну величину приходится согласовывать при каждом делении, переезде, пересборке и
перетаскивании разделителя, и каждое такое место — шов, где они расходятся. Все баги раскладки жили ровно
в этих швах: подсказка тратилась вдоль чужой оси; не переезжала вместе с узлом; доля в модели говорила
«половина», пока подсказка держала 160, и всплывала, как только подсказка переставала действовать.
Показательно, что понадобилась функция `FreezeShares`, вся работа которой — превратить одну истину в
другую: функция-переводчик между двумя источниками правды и есть доказательство, что источник должен быть
один.

Что даёт одно число:

- **Ресайз окна не трогает фиксированные панели.** Инспектор, которому сказано 240, остаётся 240, а центр
  забирает разницу — как в любом редакторе. Чистые доли этого не умеют в принципе.
- **Вытаскивание панели не двигает соседей.** Освободившееся уходит звёздам — тем, кто и сказал «беру, что
  останется».
- **Деление рядом делит длину ЦЕЛИ**, какой бы она ни была: 160 пикселей становятся 80 и 80, вес 2 —
  единицей и единицей. Пара стоит ровно столько, сколько стоил один, поэтому ряд вокруг не двигается.
- **Разделитель пишет пиксели** обоим соседям: пиксель мыши — пиксель границы, без пересчёта в доли и без
  накопления ошибки.

**Сохранённая раскладка** — файл ДАННЫХ, а не разметка; длины туда пишет программа по тому, что пользователь
натаскал, руками их не пишут нигде:

```json
{
  "version": 1,
  "roots": [
    { "main": true, "bounds": [100, 100, 1600, 900],
      "split": { "orientation": "horizontal", "children": [
        { "group": { "panes": ["scene", "game"], "active": 0 }, "length": "*" },
        { "group": { "panes": ["inspector"], "active": 0 },     "length": "240" }
      ]}}
  ]
}
```

Здесь же видно главное про структуру: **сплит вкладывается в сплит** — это и есть рекурсия узлов, а группа
всегда лист. Длина стоит на РЕБЁНКЕ, а не списком у родителя: так она путешествует вместе с тем, что
описывает, и перестановка детей не может надеть на узел размер соседа.

## Состав

### 0. Форма раскладки: ЛЕС корней, внутри — только относительное

Раскладка это **список корней**, а не одно дерево. Каждый корень несёт свой ЭКРАННЫЙ прямоугольник
(позиция + размер); внутри него узлы задают свою длину — весом в остатке или пикселями вдоль оси ряда.

```
раскладка
├─ корень (главное окно)      rect = ...   → сплиты (длины) → группы вкладок
├─ корень (плавающее окно)    rect = ...   → группа вкладок
└─ корень (плавающее окно)    rect = ...   → сплит пополам → две группы
```

Почему так, а не «дерево + отдельный список плавающих окон»:

- **Раскладка переживает смену геометрии.** Другой размер окна, другой монитор, другое разрешение —
  пересчитывать внутри нечего: звёзды сами поглощают разницу, а фиксированные панели её и не должны
  замечать (в том и смысл, что инспектор не «плывёт» вместе с окном). Пересчёт нужен только корням, и то
  лишь когда сохранённый прямоугольник оказался вне доступных экранов.
- **Плавающее окно — обычный корень**, а не особый случай. Иначе каждая операция раздваивается на «в
  главном окне» и «в плавающем», и перенос вкладки между ними становится стыковкой двух разных систем
  вместо перемещения узла.
- **Сохранять нечего сверх этого.** Корни с прямоугольниками + длины узлов + какие панели в каких группах —
  этого достаточно, чтобы восстановить раскладку полностью.

### 1. Модель раскладки (отдельно от визуала)

Дерево, а не набор контролов:

- `PaneNode` — узел; либо **split** (ориентация + дети), либо **группа вкладок** (список документов +
  активный). Своя длина есть у каждого узла — она описывает его место в РОДИТЕЛЬСКОМ ряду.
- Плавающее окно — тот же узел, просто корень своего дерева. НЕ особый случай: иначе каждая операция
  начнёт двоиться на «в окне» и «в главном».
- Модель **сериализуема**: сохранить и восстановить раскладку — половина ценности докинга. Если это
  прикручивать потом, узлы обрастут ссылками на живые визуалы и сериализовать станет нечего.

*Почему отдельно:* раскладку надо уметь менять программно (сброс к дефолтной, перспективы/режимы), и
тестировать без GPU. Дерево из чистых данных тестируется юнит-тестами, дерево из контролов — нет.

### 1a. Раскладка — СВОЯ панель, не `Grid`

Узел сплита раскладывает своя панель, а не вложенный `Grid` со сплиттерами. Причина одна и решающая:
**единственный источник истины о размерах**. У `Grid` размеры живут в определениях строк и колонок, а в
модели — свои; это две копии одного числа, которые надо синхронизировать в обе стороны (потащили
разделитель, загрузили раскладку, расщепили узел — и не перепутать индексы строк со сплиттерными). Каждая
синхронизация — место, где они расходятся. Со своей панелью длина живёт в модели и больше нигде:
`ArrangeOverride` вычитает толщину разделителей и раздаёт остаток, а перетаскивание разделителя пишет
В МОДЕЛЬ.

**Правило раздачи при этом взято у `Grid` целиком** — фиксированные сверху, остаток звёздам по весам. Спор
был не с ним, а с хранением: правило хорошее, вторая копия чисел плохая. Первая версия этого не различала,
завела рядом с долей ещё и «пиксельную подсказку» — то есть ровно ту вторую копию, от которой отказывались,
только внутри собственной панели, — и все баги раскладки выросли из швов между ними (см. «Размер узла»).

Попутно: узлов единицы, поэтому виртуализация не нужна; на каждый сплит не заводится вложенная сетка с
определениями; раскладка проверяется арифметикой прямоугольников, без GPU. **Сам разделитель
переиспользуем** (`GridSplitter`/`Thumb` — визуал и жест), заменяя только то, куда он пишет.

**Дерево нормализуется после каждой операции**, иначе вложенность растёт на ровном месте:
- сплит с ОДНИМ ребёнком схлопывается в это дитя (иначе закрытие панели оставляет пустые уровни);
- вложенные сплиты ОДНОЙ ориентации сливаются в один n-арный (иначе «влево, потом ещё раз влево» даёт два
  вложенных вертикальных сплита вместо одного с тремя детьми).

**Ограничение вложенности — по МИНИМАЛЬНОМУ РАЗМЕРУ, а не по глубине.** Расщепление отклоняется, если
любая из сторон окажется меньше `MinSize` панели. Пользователю безразличен номер уровня, ему важно, что
область стала непригодно узкой; заодно правило само подстраивается под размер окна. Предел глубины —
только страховка от программно собранных деревьев (битая раскладка, генерация в тесте), не инструмент UX.

### 1b. Авторинг: политика живёт НА КАЖДОМ ПЕЙНЕ

Термины, чтобы не путались (они про разное):
- **`Pane`** — ЕДИНИЦА докинга, его айтем: одна панель с содержимым. Их могут быть сотни, и все внутри
  одного хоста.
- **`DockingArea`** — хостит пейны и владеет раскладкой (лесом корней).
- **контейнер** — понятие `ItemsControl`: то, что генератор создаёт на элемент данных. Здесь генератор
  создаёт `Pane`, но это деталь механизма, а не синоним пейна: пейн остаётся пейном и когда написан
  руками в разметке, безо всякого генератора.

**Политика принадлежит пейну — и больше нигде её нет.** Это его собственные свойства: `Zone`, `Allowed`,
`MinSize`, `Restore`. Читаются при его создании и применяются. В разметке — обычные атрибуты:

```xml
<Pane Zone="Right" MinSize="220" Allowed="Right,Bottom,Floating">…</Pane>
```

Для пейнов, создаваемых из вьюмоделей, НИЧЕГО нового не нужно — генератор делает `Pane` на каждый
элемент, а его свойства задаются штатным `ItemContainerStyle`:

```xml
<DockingArea ItemsSource="{Binding Panels}">
    <DockingArea.ItemContainerStyle>
        <Style Selector="Pane">
            <Setter Property="Zone" Value="{Binding Placement, Mode=TwoWay}"/>
            <Setter Property="MinSize" Value="220"/>
        </Style>
    </DockingArea.ItemContainerStyle>
</DockingArea>
```

**Чего здесь СПЕЦИАЛЬНО нет** (и почему готовые докинг-контролы этим мучают):
- **адаптера «вьюмодель → тип панели»** — он нужен там, где типов панелей много (документ, тул, плавающая);
  у нас тип листа ОДИН, а «документ/инструмент» это набор значений тех же свойств. Решать нечего;
- **обязательного вью-локатора** — вид добывается штатными `ContentTemplate`/`ContentTemplateSelector`,
  теми же, что рисуют вкладки и содержимое окон. Локатор — один из возможных селекторов, а не обязанность;
- **таблицы политик по типам** — она была бы третьим механизмом и заставляла бы прописывать запись на
  каждый тип панели; свойства контейнера и селекторы стилей покрывают это без единой новой сущности;
- **базового класса, от которого надо наследоваться.**

**Размещение — СОСТОЯНИЕ, а не команда.** Поэтому оно двусторонне связываемое: пользователь перетащил —
хост записал, вьюмодель прочитала; вьюмодель присвоила `DockZone.Floating` — панель уплыла, присвоила
`DockZone.Right` — вернулась. Один сеттер на оба направления, расходиться нечему. `DockZone` — чистое
перечисление в нейтральной сборке, так что правило «в VM нет UI-типов» соблюдено: вьюмодель называет
намерение, а не конструирует UI-объект (тот же приём, что с `CursorType` в драг-дропе). Сервис с методами
`Float()`/`DockTo()` как ОСНОВНОЙ путь отвергнут: он заводит второй путь изменения раскладки рядом с
жестом, и они неизбежно разъезжаются. Сервису остаётся то, что состоянием не является — активировать,
закрыть, сбросить раскладку. Бонус: раз размещение это состояние модели, оно сериализуется само.

### 2. `DockingArea` — layout-контрол над моделью

- Строит визуал по дереву: вложенные `PaneHost` с `PaneSplitter` между ними, в листьях — `PaneGroup`.
- Реагирует на изменения модели, а не наоборот.
- Схлопывание пустых узлов (унесли последнюю вкладку — область исчезает, соседи забирают место) —
  операция модели, визуал следует.
- **Длины синхронизируются обратно в модель после раскладки.** Драг разделителя пишет в КОНТРОЛЫ — это и
  двигает границу под курсором, — и без обратной записи модель о нём не узнавала, так что следующая
  пересборка (любой дроп, любой отрыв) молча возвращала размеры до драга. Сохраняется модель, значит знать
  должна она.

### 3. Компас (`DockCompass`)

- **ОДНО** прозрачное topmost click-through окно на всю область, а не по окну на группу. Поднимается, когда
  курсор входит в область, опускается, когда выходит, и за время жеста не двигается и не меняет размер.
  Окно размером с группу приходилось двигать посреди жеста, и оно гонялось с компасом внутри: замерено, окно
  уже стало 582×464, пока компас был разложен в 582×229 — крестик строился вокруг центра прямоугольника, в
  котором его не было. Заодно края области оказались ВНУТРИ оверлея — без этого якорям по краям некуда встать.
- Показывает зоны для группы под курсором: центр — «вкладкой в эту группу», четыре стороны — «расщепить»,
  плюс края области — «пристыковать к краю» (ещё не сделано).
- Подсветка цели + предпросмотр итоговой области (полупрозрачный прямоугольник там, где панель окажется).
- Решение принимается по попаданию в **индикатор**, а не по четверти площади: у четверти нет видимых границ,
  и узнать, что сделает бросок, можно только бросив.
- **Единицы.** Экранные координаты и позиция окна — ФИЗИЧЕСКИЕ пиксели, геометрия области и компаса —
  логические. Перевод один раз, на границе (`PointerIn`). Позиция окна физическая намеренно: у точки
  десктопа нет своего масштаба, его задаёт монитор, на который она попала, — а какой это монитор, известно
  только когда точка уже физическая. Логическая позиция роняла оторванное окно в `курсор/масштаб`.

### 4. Жесты

- **Отрыв** — есть, работает.
- **Перетаскивание плавающего окна** — по `WindowMoving` показываем индикаторы, по `WindowMoveCompleted`
  стыкуем. Здесь и пригодится уже заведённый шов.
- **Перетаскивание вкладки внутри дерева** (без отрыва) — тот же компас, но жест наш, а не ОС.

### 5. Сохранение раскладки

Сериализация дерева + идентификаторы панелей (какая вьюмодель/вид где лежал), восстановление при старте.

---

## Порядок

1. **Модель + сериализация**, с юнит-тестами: расщепить, схлопнуть, перенести узел, сохранить/загрузить.
   Без визуала вообще — это самая дешёвая и самая ошибкоёмкая часть.
2. **`DockingArea`** поверх модели: отрисовка дерева, сплиттеры, вкладки в листьях. Пока без перетаскивания.
3. **Компас** и стыковка ПЕРЕТАСКИВАНИЕМ ВКЛАДКИ внутри одного окна (наш жест, ввод наш).
4. **Стыковка плавающего окна** — по `WindowMoving`/`WindowMoveCompleted`, поверх готового компаса.
5. **Сохранение/восстановление** раскладки.
6. Отделка: зоны у краёв, автоскрытие панелей, вкладочные документы против панелей инструментов.

Первые два пункта не требуют ни одного нового пикселя на экране и полностью покрываются тестами — начинать
надо с них.

---

## Документы против инструментов — РЕШЕНО: это политика панели, а не тип узла

Различие настоящее, и держится оно на РАЗНОМ ЖИЗНЕННОМ ЦИКЛЕ, а не на внешнем виде:

- **Восстановление.** Инструменты — часть рабочего пространства, восстанавливаются при старте. Документы —
  часть сессии; восстанавливать их так же нельзя, файла может уже не быть. Слей их в одно — и загрузка
  раскладки станет воскрешать документы, которых больше нет.
- **Место.** Центр — «то, что редактирую», края — «то, что помогает редактировать». Без различия у
  раскладки нет понятия центра, и каждый новый документ открывается где придётся, а область редактирования
  уезжает под пользователем.
- **Закрытие.** Инструмент закрывают = прячут (вернётся из меню). Документ закрывают = «сохранить
  изменения?». Две разные операции под одной кнопкой — плохая идея.

**Но двух типов узлов не будет.** Жёсткая пара «документ/инструмент» не описывает третий, вполне реальный
случай: маленькое окошко параметров, которое открыли, покрутили с живым откликом и тут же закрыли (так
устроены редакторские утилиты в движках уровня Ubisoft). Оно не документ и не пристыкованный инструмент.

Поэтому в дереве остаётся ОДИН вид листа (группа вкладок) и сплиты, а различия становятся **свойствами
панели**: восстанавливается ли с раскладкой, где ей разрешено стыковаться, тяготеет ли к центру, что
значит её закрытие. «Документ» и «инструмент» — просто два типовых набора этих свойств, эфемерная
утилита — третий. Новый вид панели не требует нового типа узла и не трогает модель.

**Уточнено 2026-07-30 (правило 1 свода):** политика принадлежит панели, но ОБЛИК — месту. Инструмент,
брошенный в центральную область, выглядит и ведёт себя как документ, потому что зоны инструментов — строго
по краям контрола, а сворачивать панель в центре некуда. Одно другому не противоречит: политика решает,
куда панель пускать и что значит её закрытие, место решает, во что она одета.

## Владение плавающими окнами — РЕШЕНО: ими владеет докинг

Оторванная панель остаётся **узлом того же леса** — просто корнем со своим окном. Как только она
становится «чем-то отдельным», раздваивается всё остальное: сохранение перестаёт покрывать плавающие
панели (а пользователь ждёт, что инспектор, оставленный на втором мониторе, там и окажется после
перезапуска), перенос вкладки обратно превращается в стыковку двух систем, «сбросить раскладку» получает
двух хозяев.

Возражение «элемент интерфейса не должен владеть окнами верхнего уровня» на практике не работает:
всплывающие меню, подсказки и наш `OverlayWindow` ровно этим и заняты — прецедент в движке уже есть.

Разделение: **модель** владеет узлом, **хост** материализует его в окно и отвечает за время жизни окна,
**приложение** через тот же крючок, что и у отрыва вкладки сейчас, говорит, каким это окно должно быть
(рамка, размер).

## Ограничения стыковки — набор ЗОН, плюс отменяемое событие для редкого

Панель должна уметь сказать «сюда меня класть нельзя» (вьюпорт не место в узкой боковой колонке). Словарь
выбран самый простой из работающих — **набор разрешённых зон** (центр / лево / право / верх / низ /
плавающей): это данные, они сериализуются и читаются глазами. Он грубоват — им не выразить «только внутри
левой колонки» или «не рядом вон с той панелью», — и для таких случаев служит уже заложенное отменяемое
событие: приложение говорит «нельзя», не залезая внутрь контрола. Предикат вместо набора зон отвергнут:
он выражает что угодно, но не сериализуется, не виден глазами и прячет поведение в код — ровно та
мутность, от которой мы уходим.

## Свод правил поведения

Правила пронумерованы, чтобы на них можно было ссылаться в коде, в тестах и в разговоре. У каждого раздела помечен
статус: **есть** — работает и покрыто тестами, **частично** — работает не во всех случаях, **нет** — правило
принято, но не реализовано. Правило без проверки — это пожелание, поэтому каждое сформулировано так, чтобы по нему
можно было написать тест; большинство уже написаны (`Tests/Adamantium.UITests/DockAutoHideTests.cs`,
`DockingLayoutTests.cs`, `DockingAreaTests.cs`).

### 0. Словарь

| Термин | Что это | Тип |
|---|---|---|
| **Панель** (pane) | один лист содержимого: сцена, инспектор, консоль | `Pane` |
| **Группа** | несколько панелей как вкладки; ЛИСТ дерева, группы не вкладываются | `PaneGroupNode` / `PaneGroup` |
| **Разбиение** | ряд узлов вдоль одной оси с делителями между ними | `PaneSplitNode` / `PaneHost` |
| **Корень** | верх одного дерева; их несколько — главное окно плюс плавающие | `DockingRoot` |
| **Раскладка** | ЛЕС корней и все операции над ним; чистые данные, без контролов | `DockingLayout` |
| **Область** | контрол, показывающий ОДИН корень | `DockingArea` |
| **Зона** | куда целятся при броске: центр / лево / право / верх / низ | `DockZone` |

Ключевое: **модель не знает про окна.** Она знает про корни. Окно — это способ показать корень, и именно поэтому
плавающая панель ничем не отличается от пристыкованной с точки зрения любой операции.

---

### 1. Облик определяется МЕСТОМ, а не панелью

Статус: **есть** (2026-07-30). Документная область — `DockingLayout.DocumentWell`; `PaneGroup.Kind` пушится из
модели при каждой пересборке, как `Edge` и `State`.

1.1. Панель несёт **политику** (`Pane.Kind`, разрешённые зоны, что значит её закрытие, восстанавливается ли с
раскладкой). Политика — про жизненный цикл и про то, куда панель пускать.

1.2. **Облик группы** — шапка, где вкладки, есть ли пин — определяется тем, ГДЕ группа стоит, а не тем, что в ней
лежит. Инструмент, брошенный в центральную область, выглядит и ведёт себя как документ: вкладка сверху, шапки нет,
пина нет. Иначе центр перестаёт быть центром — в нём заводится колонка с каптионом и кнопкой сворачивания, а
сворачивать её некуда.

1.3. Формально: в главном корне есть **документная область** (document well) — узел, который является домом
документов. Всё, что оказалось внутри неё, — документная группа; всё за её пределами — инструментальная.

1.4. Документная область **не сворачивается, не отрывается и не исчезает** — опустев, она остаётся пустым местом.
Не исчезает и по размеру: сколько бы панелей ни пристыковали к её краям, за ней остаётся её пол (п. 7.6).
Иначе «закрыл последний документ» означало бы «потерял центр раскладки», и следующий документ открылся бы где
придётся.

1.5. Политика при этом не теряется: инструмент, ставший на вид документом, остаётся инструментом для сохранения
раскладки, для меню «Окна» и для смысла своего закрытия.

1.6. Документная область в первой версии — ОДНА группа. Разбиение документной области (два документа рядом,
как в VS) сюда ещё не входит: чтобы `well` расширялся на новый сплит, нужно знать род перемещаемой панели в
операциях модели, а она оперирует идентификаторами. Дорога не закрыта — `DocumentWell` уже узел, а не флаг.

---

### 2. Зоны и что куда можно бросить

Статус: **есть**.

2.1. Бросок в **центр группы** делает панель её вкладкой. Бросок в любую другую зону **разбивает** цель и ставит
панель рядом.

2.2. Цель броска — любой УЗЕЛ, не только группа. Прицел в край области нацелен на КОРЕНЬ — вот почему боковой док
идёт во всю высоту, и почему это та же операция, а не вторая.

2.3. Боковые панели делят **корень** и потому идут во всю высоту; верх/низ делят **центральную колонку** и потому
не проходят под боковыми. Это порядок каждого редактора, и он зафиксирован в построении из зон, а не зависит от
порядка объявления в разметке.

2.4. Панель может объявить **набор разрешённых зон** (`Pane.Allowed`). Прицел в запрещённую зону не подсвечивается
и бросок туда не происходит — панель просто возвращается. Панель, которой не разрешено `Floating`, не отрывается
вообще: полоса удерживает её и продолжает переупорядочивание.

2.5. Разрешение группы — **пересечение** разрешений её панелей. Одна панель, запрещающая сторону, запрещает её
всей группе: бросок группы положил бы туда и её тоже, а разрешение, которое перестаёт действовать, когда панель
едет в компании, — не разрешение.

2.5a. **Контрол спрашивает, а не решает.** Приложение может отказать двумя отменяемыми событиями на
`DockingArea`: `PaneDocking` — перед стыковкой, `PaneTearingOff` — перед отрывом в отдельное окно (перед
закрытием такое событие уже было — `TabControl.TabCloseRequested`). Отказ означает «как будто жест не
состоялся»: вкладка возвращается в полосу, панель остаётся докнутой, плавающее окно стоит где стояло.

Два события, а не одно: отказать в перемещении ВНУТРИ окна и отказать в окне СВОЁМ — разные утверждения о
панели (вьюпорту, которому нужен главный swap chain, можно ездить по раскладке, но нельзя улетать).

События поднимаются **на действие, а не на движение мыши**: компас показывает то, что разрешают ЗОНЫ, а
вопрос, задаваемый сотни раз в секунду — с тем, что приложение делает в обработчике, — это не вопрос, на
который можно честно ответить. Разделение то же, что и в п. 2.4: набор зон отвечает «куда вообще можно» и
сериализуется, событие — «а вот сейчас нельзя» и живёт в коде.

Спрашивают у ГЛАВНОЙ области: плавающее окно — та же система докинга в другом окне, и приложение подписывается
один раз, а не на каждое окно, которое оно не просило открывать.

2.6. **Убранная панель — не цель броска.** От неё осталась полоса кнопок: тела, в которое можно было бы вложить
вкладку или которое можно разбить, там нет. И эти полосы стоят вдоль краёв — ровно там, куда целятся в панель за
ними, — так что крест, нарисованный на полосе, ложится поверх креста настоящей панели, и ни один из них больше не
говорит, куда попадёт бросок. Краевые якоря при этом работают: они принадлежат области, а не полосе.

2.7. Ничто не бросается **в самого себя**: цель, лежащая внутри перемещаемого узла, — отказ, а не перестроение.

2.8. Содержимое **главного корня целиком** никуда не бросается: тащить главное окно — значит двигать окно. То же
про документную область: она не сворачивается, не отрывается и не переносится.

---

### 3. Три состояния группы

Статус: **есть**.

Состояния: `Docked` → пристыкована; `Collapsed` → убрана к краю, видна только полоса вкладок; `Revealed` → полоса
осталась у края, тело показано.

3.1. **Пин** переключает `Docked` ⇄ убранное состояние, причём из ЛЮБОГО убранного: показанная панель в одно
нажатие становится пристыкованной.

3.2. **Клик по вкладке убранной полосы** ПОКАЗЫВАЕТ панель, но не пристыковывает её. Это разница между «взглянуть
на инструмент» и «держать его открытым», и ровно поэтому состояний три, а не флаг.

3.3. **Клик мимо** показанной панели убирает её обратно. Пин — единственное, что удерживает.

3.4. Убранная панель **не имеет выделенной вкладки**: полоса — это ряд кнопок, а подсвеченная кнопка утверждала бы,
что панель открыта. Показ выделяет нажатую; пин возвращает обычную полосу, которая выбирает вкладку сама.

3.5. Свернуть **единственную группу корня** нельзя — останется окно из одних полосок.

3.6. Убранная панель **остаётся на своём месте в дереве** и занимает ровно столько, сколько намерила её полоса
(`Auto`). Размер, который у неё был, запоминается и возвращается при пине.

3.7. Свёрнутая у БОКОВОГО края панель поворачивает надписи вкладок наружу; свёрнутая сверху или снизу — нет,
она и так широкая полоса.

3.8. Панель, **потерявшая край, разворачивается**. Быть убранной — это утверждение о крае: полоса живёт на нём,
надписи повёрнуты наружу. Стоит задокировать что-нибудь СНАРУЖИ такой панели — и края у неё больше нет, значит
нет и фолда. Замерено до исправления: панель оставалась свёрнутой, шапка и тело так и не показывались, а полоса,
съехав с края, разворачивалась горизонтально — на экране оставалась пустая коробка с вкладками снизу и без тайтла.

3.9. **Убранную панель нельзя ресайзить.** Её размер МЕРЯЕТСЯ — полоса, и ничего больше, — а не назначается, поэтому
разделитель рядом с ней не является ручкой: зазор остаётся ради шва, но не тянется. Замерено: перетаскивание писало
пиксели на контрол, чья модель по-прежнему говорит `Auto`; модель их отвергала (`OwnsLength`), пересборки не
происходило, и панель просто оставалась растянутой — с уже спрятанными шапкой и телом. Это же и есть та «пустая
коробка с вкладками», которую видно на скриншотах.

3.10. Показанная панель **накрывает соседей, а не раздвигает их**. Статус: **есть** (2026-07-30).

Устройство: **полоса остаётся в дереве, а СОДЕРЖИМОЕ активной панели переезжает в `Popup`**. В разбиении
показанная группа ведёт себя ровно как убранная — `Auto`, одна полоса, — поэтому дерево не меняется вообще
(`RevealGroup` длину не трогает, `OwnsLength` истинно только для `Docked`). Попап прижат к полосе
(`PlacementTarget`), берёт поперёк `RestoreLength` (доля переводится в пиксели: флайаут размещают, а не делят
между соседями), а вдоль — длину самой полосы. У флайаута своя шапка с пином и крестиком: докнутая в этом
состоянии не на экране, а флайаут, который нельзя закрепить, — это панель, на которую можно только смотреть.

Три вещи, которые пришлось узнать по дороге:

- **Содержимое нельзя держать в двух местах.** Презентер держит своего визуального ребёнка даже со скрытым
  телом, поэтому у группы два свойства — `DockedContent` и `FlyoutContent`, — и ровно одно из них не пусто.
  Обнулять `Content` из триггера нельзя: `{x:Null}` в AUML нет, а локальная запись перебьёт привязку шаблона
  и не вернёт её.
- **Размер задаётся содержимому попапа, а не попапу.** Попап — элемент дерева независимо от того, открыт он
  или нет, и заданная ему ширина отбирает место у полосы, рядом с которой он объявлен.
- **`IsOpen` ведёт КОНТРОЛ, а не триггер.** Триггер надо откатывать, чтобы закрыть попап, а откат идёт через
  `ClearValue`, который роняет колбэк (см. `TECH_DEBT.md`) и уносит с собой остаток метода.

Гашение по клику мимо принадлежит попапу (`KeepOpen=false`), а не области: тело живёт в попап-слое окна, вне
поддерева группы, поэтому прежний вопрос области «нажали внутри группы?» считал бы нажатие по собственному
содержимому панели нажатием мимо.

Отвергнуто: выносить в оверлей ГРУППУ целиком с распоркой на её месте в дереве. Это требует мерить толщину
полосы, заводить распорку и задавать порядок проходов раскладки — много механики ради того же результата.

---

### 3a. Что значит закрыть

Статус: **есть** (2026-07-30), кроме меню возврата — оно за приложением.

3a.1. **Документ закрывают — он уходит.** Он был частью сессии, и файла в следующий раз может уже не быть.

3a.2. **Инструмент закрывают — его ПРЯЧУТ.** Он часть рабочего пространства и обязан возвращаться:
`DockingArea.HiddenPanes` перечисляет спрятанное, `RestorePane(id)` возвращает. Одна кнопка, два глагола, и
который из них — говорит `Pane.Kind`, потому что это политика, а политика принадлежит панели (правило 1.5).

3a.3. Спрятанный инструмент возвращается **на своё место** — в ту же группу и на тот же индекс. Если группа с тех
пор умерла, он идёт в зону, которую ему дал автор.

3a.4. Закрытие **отменяемо** (`TabControl.TabCloseRequested`) — там живёт «сохранить изменения?».

3a.5. В докинге строка вкладок НЕ удаляет вкладку сама: истина — в модели, полоса догоняет на следующей
пересборке. Удаление в обоих местах вынимало бы панель дважды.

### 3b. Убранные панели живут в ПОЛОСАХ КРАЁВ, а не в дереве разбиения

Статус: **есть** — переделано 2026-07-31 (`DockingRoot.Bars`, `DockingLayout.CollapseGroup`/`ExpandGroup`,
`BarredGroups`, раскладка полос в `DockingArea.ArrangeBar`). Всё, что ниже в §3, описывает поведение; ЭТОТ пункт
описывает, где убранная панель хранится, и он важнее: прежнее хранение было причиной целой цепочки дефектов.
Проверено замером: за прогон ни одна свёрнутая группа не размещалась деревом — все числятся полосой своего края.

3b.1. У каждого корня есть **четыре полосы автоскрытия** — левая, верхняя, правая, нижняя. Убранная панель лежит
В ПОЛОСЕ, а не в дереве разбиения. Пин вынимает её из дерева и кладёт в полосу того края, у которого она стояла;
обратный пин возвращает её в дерево на прежнее место с прежним размером.

3b.2. Полосы **не участвуют ни в чём**, что делает дерево: их не задевают дропы, они не цели стыковки, их не делят
разбиения, они не двигаются и не меняются, когда в дереве что-то происходит. Это отдельный слой по краям области,
а внутри — только дерево.

3b.3. Показанная панель — это панель ИЗ ПОЛОСЫ, чьё тело показано флайаутом (правило 3.10). В дереве её нет ни в
каком виде.

**Почему это была переделка, а не правило.** Убранная группа оставалась узлом дерева с длиной `Auto`, и каждое
следствие этого чинилось отдельно: она разворачивалась, когда дерево нормализовалось (правило 3.8); её нельзя было
сворачивать не у края (3.5), потому что «край» — свойство позиции в дереве; её разделитель тянулся и писал ей
пиксели (3.9); нижний якорь резал её по высоте (2.3), потому что делил корень вместе с ней. Все четыре — одна
ошибка: панель, которой в раскладке нет, лежала в структуре раскладки. Вынос в полосы снял все четыре разом, а
`Unfold`, запрет «не у края» и `IsPutAway` у разделителя исчезли вместе с причиной.

**Что осталось от этого пункта:** сохранение раскладки должно писать полосы отдельно от дерева — это разные вещи.

### 4. Плавающие окна

Статус: **есть** (кроме 4.6).

4.1. Плавающая панель — **корень того же леса**, а не отдельное окно с панелью внутри. Поэтому она сохраняется с
раскладкой, возвращается той же операцией, что и всё остальное, и «сбросить раскладку» имеет одного хозяина.

4.2. Окно держит **область докинга**, а не голое содержимое. Отсюда следует всё остальное: у него есть полоса
вкладок, свой компас, и в него можно докидывать.

4.3. Группа, составляющая ВСЁ содержимое плавающего окна, **шапки не носит**: заголовок окна уже называет её, а
пину там нечего делать — края, к которому сворачивать, нет. Разбей окно на две панели — шапки возвращаются, потому
что теперь они единственное, что их различает и за что их таскать.

4.4. Заголовок такого окна **следует за активной вкладкой**: отдав шапку, панель осталась без другого места, где
пишется её имя.

4.5. Опустевшее плавающее окно **закрывается само** — и когда из него утащили последнюю вкладку, и когда его
содержимое пристыковали обратно.

4.6. Панель, оказавшись в плавающем окне, **не свёрнута**: фолд описывал край, которого больше нет. Пин в
одиночном плавающем окне недоступен.

---

### 5. Жесты

Статус: **есть**.

5.1. **Вкладка вдоль полосы** — переупорядочивание. **Вкладка поперёк полосы, дальше порога** — отрыв. Два жеста
различаются НАПРАВЛЕНИЕМ, а не скоростью и не таймаутом; порог по умолчанию — собственная толщина полосы, так что
рука, гуляющая на пару пикселей вбок, ничего не отрывает.

5.2. **Шапка инструментальной группы** тащит ВСЮ группу, со всеми панелями и их порядком. Вкладка двигает панель,
шапка — панель целиком: какой это жест, решает место нажатия, а не число открытых вкладок.

5.3. Пересечение порога **И ЕСТЬ** отрыв: окно появляется там и тогда, а не висит полу-оторванным до отпускания
кнопки. Иначе полоса продолжала бы держать место под то, что уже ушло, а окно, в которое надо целиться, появлялось
бы после того, как целиться уже поздно.

5.4. Дальше жест ведёт **системный цикл перемещения окна** — с Aero Snap и без пересчёта позиции на каждое
движение мыши.

5.5. **Заголовок плавающего окна** тащит окно; брошенное на компас, оно возвращается в дерево целиком — вместе со
всем, что успели в него сложить.

5.6. Кнопки в шапке (пин, закрыть) **гасят нажатие** и потому никогда не превращаются в отрыв.

---

### 6. Что рисуется в каком состоянии

Статус: **есть**.

| | шапка | вкладки | пин | индикатор выделения |
|---|---|---|---|---|
| документная группа | нет | сверху | нет | есть |
| инструмент, пристыкован | есть, акцентная | снизу | есть | есть |
| инструмент, убран | нет | у края, повёрнуты | нет — шапки нет; вернуть кликом по вкладке | нет (нечего выделять) |
| инструмент, показан | есть | у края, повёрнуты | есть | есть |
| один в плавающем окне | нет (заголовок окна) | снизу | нет | есть |
| несколько в плавающем окне | есть | снизу | нет | есть |

6.1. Полосы выделения — как у обычного `TabControl`. Инструментальная группа без индикатора не даёт понять, какая
панель открыта, иначе как по её содержимому.

6.2. Повёрнутые надписи на ОБОИХ боках читаются **сверху вниз** — один угол на обе стороны, как в Visual Studio.
Разворачивать их «наружу своей панели» (слева снизу вверх, справа сверху вниз) выглядит симметрично на словах, а
на экране читается как два разных соглашения в одном окне.

---

### 7. Размеры

Статус: **есть**.

7.1. Автор пишет **ЗОНУ, а не долю**: доля была бы долей разбиения, которого автор не видит, и перестала бы быть
правдой при первом же движении делителя.

7.2. Автор МОЖЕТ написать стартовый размер в пикселях вдоль оси зоны. Это подсказка для первой раскладки.

7.3. Разбиение делит длину ЦЕЛИ пополам, а не забирает у соседей: пара стоит ровно столько, сколько стоил один, и
ряд вокруг не двигается. Исключение — краевой док, который берёт полосу заданной ширины и цель не трогает.

7.4. Размеры возвращаются из контролов в модель **только по окончании перетаскивания делителя**. Модель — истина,
контролы — её вид; копирование на каждом проходе позволяло свежепостроенному контролу записать свой умолчательный
`Star` поверх настоящего размера.

7.5. Убранная панель имеет длину `Auto` — сколько намерила полоса. Число здесь было бы вторым мнением о высоте,
которую полоса уже знает, и разошлось бы с ней при первом изменении вкладок.

7.6. **Документная область никогда не делится пополам и не может быть выдавлена.** Инструмент, вставший рядом с
ней, берёт ПОЛОСУ (`DockingLayout.BandLength`, приходит из `DockingArea.EdgeDockSize`), а центр оставляет себе
остаток: центр теряет ширину пришедшего, а не половину того, что имел. Половина означала 1/2 → 1/4 → 1/8, и через
несколько бросков в один край центр исчезал с экрана. Плюс у центра есть пол — `DockingArea.DocumentMinSize`
(умолчание 200): раскладка не даёт ему уйти ниже, а компас не предлагает зону, которую центру нечем оплатить —
тем же механизмом, что гасит запрещённые зоны `Pane.Allowed` (п. 2.5). Так это устроено и в промышленных
контролах: документы получают долю, инструменты — полосу, у редакторской области есть минимум.

---

### 8. Инварианты раскладки

Статус: **есть**.

8.1. Пустая группа исчезает. Разбиение с одним ребёнком схлопывается в это дитя. Разбиение внутри разбиения той же
ориентации сплющивается. Доли пересчитываются в сумму 1.

8.2. Опустевший корень удаляется — **кроме главного**: приложения без главного окна не существует, и такого
состояния раскладки быть не должно.

8.3. Панель — ОДИН контрол, и живёт она ровно в одном дереве. Переезжая между окнами, она сначала уходит из
старого дерева и только потом приходит в новое.

8.4. Каждая операция кончается перестроением ИЗ МОДЕЛИ. Ни один жест не правит контролы напрямую, иначе модель и
экран станут двумя ответами на один вопрос.

---

### 9. Навигация: докинг как РЕГИОН

Статус: **есть** (2026-07-30) — `DockingAreaRegionAdapter`, зарегистрирован на `DockingArea` в
`UIApplication`. Не сделано только меню «Окна» поверх `HiddenPanes` (п. 9.5).

9.1. `DockingArea` должна быть **регионом навигации**: `nav:RegionManager.RegionName="Documents"` на области, и
вьюмодель открывает вид через `Regions["Documents"]`, не зная ни про вкладки, ни про панели. Без этого докинг —
остров: открыть документ можно только руками, и весь навигационный слой (`INavigationService`, `IViewLocator`)
проходит мимо главной части редакторского UI.

9.2. **`DockingAreaRegionAdapter`** — то, чего не хватает. Что он обязан отображать:

| Регион | Докинг |
|---|---|
| добавить вид | новая `Pane` в **документной области** (правило 1), или в зону, объявленную политикой вида |
| активировать вид | выбрать его вкладку; если панель убрана — **показать** её (правило 3.2) |
| активный вид | выбранная вкладка активной группы |
| убрать вид | закрытие панели по её политике (правило 3a): документ уходит, инструмент прячется |
| виды региона | все панели области, включая плавающие окна — они корни того же леса |

9.3. `PaneGroup` — это `Selector`, поэтому существующий `SelectorRegionAdapter` уже годится, если регион вешают
на ОДНУ группу. Отдельный адаптер нужен именно области: у неё есть выбор МЕСТА, которого у полосы вкладок нет.

9.4. Навигация к уже открытому виду **не открывает второй раз** — она активирует существующую панель. Иначе
«перейти к файлу» плодит копии, а докинг не может сказать, что вид уже где-то открыт.

9.5. Спрятанные инструменты (`HiddenPanes`) — тоже виды региона, просто неактивные: возврат из меню «Окна» и
навигация к ним должны быть одной дорогой, а не двумя.

### 10. Открытые дефекты

- **4K: окно уезжает от курсора при отрыве.** На масштабированном мониторе оторванное окно оказывается далеко от
  курсора (замерено на 4K). Позиция окна — физические пиксели, размеры контролов — логические; где-то на этом стыке
  теряется масштаб. Разбирать отдельным заходом, вместе с `PER_MONITOR_DPI_PLAN.md`; ориентир — как это сделано в
  Avalonia.
- **`ClearValue` роняет обработчики свойств** — общий дефект ядра, найден на `Popup.IsOpen`; подробности и
  замер в `TECH_DEBT.md`. В докинге обход простой: `ClearValue` не используется.
- **Облик не выводится из места** (раздел 1) — инструмент, брошенный в центр, остаётся на вид инструментом.
- **Полоса прокрутки после смены темы** — см. `TECH_DEBT.md`.

---

## Открытые вопросы

- **Документная область как явный узел.** Правило 1 требует, чтобы облик выводился из места, а для этого
  раскладке нужен помеченный узел-дом документов: он не сворачивается, не отрывается и, опустев, остаётся
  пустым местом. Открыто, как он переживает `Normalize` (схлопывание разбиения с одним ребёнком) и что
  происходит с ним при загрузке старой раскладки, где такой пометки нет.

Остальное, что было открыто, решено выше. Дефекты, а не вопросы дизайна, — в §10 свода правил.

---

## Что осталось сделать для полной реализации

Состояние на 2026-07-30. Работает: модель (лес корней, разбиения, группы, `Split`/`MovePane`/`MoveNode`/
`TearOffGroup`/`Normalize`), `DockingArea` с `PaneHost`/`PaneGroup`/`PaneSplitter`, компас с краевыми якорями,
отрыв вкладки и отрыв группы за шапку, плавающее окно как полноценная область докинга (докидывание внутрь и
возврат броском), три состояния панели с пином и авто-уходом, тема FluentDark.

Отдельно отмечено **«объявлено, не работает»** — свойство в API есть, а поведения за ним нет. Это худшая из
категорий: снаружи выглядит как готовая возможность.

### A. Без этого контрол нельзя считать готовым

0. **Убранные панели — в ПОЛОСЫ КРАЁВ, вон из дерева разбиения** (правило 3b). Первым номером, потому что это
   причина, а всё, что чинилось вокруг неё, — следствия. Пока панель, которой в раскладке нет, лежит в структуре
   раскладки, каждый новый вид дропа будет находить новый способ её задеть.

1. **Сохранение и загрузка раскладки.** Читаемый текст с версией (раздел «Сохранение раскладки» выше). Сейчас
   нет ничего: `DockingLayout.Version` объявлен, сериализации нет, `DockingRoot.Bounds` (экранный прямоугольник
   корня) **объявлен и нигде не используется** — то есть плавающее окно, оставленное на втором мониторе, не
   вернётся туда после перезапуска. Это то, ради чего плавающие окна вообще сделаны корнями леса.
2. ~~Ограничения стыковки~~ — **сделано 2026-07-30**: `Pane.Allowed` проверяется в `DockingArea.Resolve`
   (запрещённая зона не вооружается) и в обоих отрывах; разрешение группы — пересечение разрешений её панелей.
3. ~~Отменяемые события перед стыковкой и отрывом~~ — **сделано 2026-07-30** (правило 2.5a): `PaneDocking` и
   `PaneTearingOff` на `DockingArea`. Тестами не покрыты: оба поднимаются внутри жестов, а жест открывает
   окно и ходит через диспетчер — то же ограничение, что и у пункта 13 ниже.
4. ~~Закрытие по политике~~ — **сделано 2026-07-30** (правило 3a): документ уходит, инструмент прячется в
   `HiddenPanes` и возвращается `RestorePane(id)`. Остаётся **меню «Окна»** в приложении — без него список
   спрятанного некому показать. `Pane.Restore` (восстановление с сохранённой раскладкой) по-прежнему **объявлен
   и не используется** — он про сохранение, то есть про пункт 1.
5. ~~Документная область~~ — **сделано 2026-07-30** (правило 1): `DockingLayout.DocumentWell`; облик группы
   пушится из модели. Не сделано: **разбиение документной области** на несколько групп (правило 1.6).

### B. Поведение, обещанное сводом правил

6. ~~Показанная панель накрывает соседей~~ — **сделано 2026-07-30** (правило 3.10): тело уезжает во флайаут,
   полоса остаётся в дереве. Попутно починен **`{Binding ElementName=…}` внутри `ControlTemplate`**: шаблон —
   свой namescope, его имена не лежат на дереве под `Name`, поэтому такая привязка молча оставалась без
   источника. Резолвер спрашивает шаблон владельца первым (`ITemplateHost` в Core — `GetTemplateChild` живёт в
   Controls, на которые Core не ссылается).
7. **Прицел в ПОЗИЦИЮ внутри полосы вкладок.** `MovePane` принимает `index`, но компас умеет целиться только «в
   центр группы», то есть вкладка всегда встаёт последней. Готовый ответ на «куда встанет вкладка» уже есть —
   `VirtualizingPanel.DropGapIndex` + `DropPlaceholderTemplate`.
8. ~~Пружинная активация~~ — **сделано 2026-07-30**: задержка перетаскивания над чужой вкладкой выводит её
   вперёд (`DockingArea.SpringLoadDelay`, по умолчанию 600 мс). Именно задержка, а не мгновенная подмена:
   полосу перетаскивание ПЕРЕСЕКАЕТ по пути к компасу, и подмена тела под каждой пройденной вкладкой
   пролистывала бы всю группу.
9. ~~`DockingWindow` как свой тип~~ — **сделано 2026-07-30**. Чем окно плавающей панели ЯВЛЯЕТСЯ — хром, стартовый
   размер (`FloatingWindowWidth`/`Height` на области), как его одевает тема — теперь сказано в одном месте, а не
   отвечается в каждом из двух жестов, которые окно открывают. Ловушка: селекторы стилей матчатся по ТОЧНОМУ
   типу, поэтому теме нужна явная строка `<Style Selector="DockingWindow" BasedOn="Window"/>` — без неё окно
   остаётся вообще без шаблона.
10. ~~`DockingAreaRegionAdapter`~~ — **сделано 2026-07-30** (правило 9). Вьюмодель, к которой навели, становится
    панелью в ДОКУМЕНТНОЙ ОБЛАСТИ; повторный переход к уже открытой активирует её, а не заводит вторую; клик
    по вкладке — тоже навигация и доезжает обратно в регион, иначе экран и журнал расходятся. Для этого у
    области появился код-путь к тем же операциям: `AddPane`/`Activate`/`RemovePane`/`Panes` и событие
    `ActivePaneChanged`. **Остаётся меню «Окна»** поверх `HiddenPanes` — оно должно быть той же дорогой, а не
    второй.

### C. Платформа, тема, качество

10. **DPI: оторванное окно уезжает от курсора на масштабированном мониторе** (§10, `TECH_DEBT.md`). Стык
    физических и логических координат в `DockingArea.Show`/`PointerIn`/`Resolve`.
11. ~~**Светлая тема.**~~ Сделано (проверено 2026-07-31). Пункт был написан по неверной посылке: `StyleSet`-файлы
    ОБЩИЕ для обеих тем, `FluentLight.auml` подключает `DockingStyleSet` наравне с остальными, а различаются только
    палитры. Проверено: в `DockingStyleSet` нет ни одного захардкоженного цвета (единственный литерал —
    `Fill="Transparent"`); все 10 токенов, которые он просит, разрешаются в светлой теме — четыре собственных
    (`DockFoldedStripFill`, `DockCompassFillColor/StrokeColor/PreviewColor`) заданы в `BrushesLight.auml` со
    ИНВЕРТИРОВАННЫМИ значениями, остальные приходят из общей палитры и из акцента, который тема выводит из своего
    семени (`#005FB8` для светлой).
12. ~~**Клавиатура и фокус.**~~ Перенесено в `TECH_DEBT.md` (раздел «Ввод с клавиатуры») 2026-07-31: управления с
    клавиатуры нет ни в одном контроле движка, так что это общий слой, а не работа докинга. Докинг — первый заказчик.
13. **Тесты уровня контролов на новые жесты.** Частично (2026-07-31): `DockingGestureTests` покрывает всё, что
    решается ДО открытия окна — отказ приложения на отрыв (`PaneTearingOff`), отказ по правам (`Pane.Allowed`, в том
    числе «одна панель держит всю группу»), закрытие документа и инструмента с `PaneClosed`/`HiddenPanes` и возврат
    через `RestorePane`. `DockingSizingTests` — пол документной области, соблюдение минимумов при раздаче,
    добавление панели вкладкой на занятую сторону, отсутствие утечки контролов.
    ОСТАЛОСЬ: половина жеста, которая ОТКРЫВАЕТ окно (отрыв, дроп окна на компас, возврат). Ей нужен
    `UIAppContext`/диспетчер, которых в юнит-тестах нет — либо поднимать тестовый контекст приложения, либо выносить
    создание окна за интерфейс, который тест подменит.
14. **Снять временную диагностику.** `PaneGroup.Log()`, `Pane.MeasureCount`/`LastMeasureConstraint`, вывод
    `[TabTear]`/`[DockTearOff]`/`[DockTrack]`/`[PIN]` и временная строка выбора вкладки в `GalleryViewModel`.
15. ~~**`_hostsByNode` не чистится.**~~ Сделано 2026-07-31: `Prune` собирает живые узлы ОБОИХ видов и забывает
    контролы и групп, и разбиений (`Forget`). Замерено тестом `ControlsOfDeadNodesAreForgotten`: пять циклов
    «открыть панель сбоку → закрыть» оставляли 5 осиротевших хостов, теперь 0.
