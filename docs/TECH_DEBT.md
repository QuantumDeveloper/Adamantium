# Технический долг Adamantium.UI — что недоделано

_Только открытое. Закрытое отсюда удаляется: разбор закрытых заходов живёт в планах (`docs/*_PLAN.md`) и в истории коммитов. Перед тем как брать пункт — сверь с кодом, заметки протухают._

Приоритеты: **P1** мешает уже сейчас · **P2** нужно для качества/перфа · **P3** будущее/дизайн.

---

## Opacity

- **[P2] Альфа слота не подключена к element-`Opacity`.** Таблица ПОСТРОЕНА: `NodeSlot.Params.x` — альфа на узел, `TransformTable.SetAlpha` её пишет, шейдер читает живьём. Вызывающих `SetAlpha` в UI нет ни одного, поэтому смена/анимация `Opacity` по-прежнему = перекрас слотов O(поддерева)/кадр. Работа не «построить таблицу», а найти ей источник значения. `RENDER_CACHE_REDESIGN.md` §7, `OPACITY_MODEL_PLAN.md` §6.
- **[P2] Композиторный канал для element-opacity.** Анимированная element-opacity тикает на loop, не на рендер-потоке. `Compositor.TryTakeOver` для Paint ждёт Brush-таргет. Полный off-loop = таблица выше + канал.
- **[P2] `LayerOpacity` / offscreen-слой.** Групповая `Opacity` двоит альфу на пересечениях детей — настоящего слоя нет. Тянет общую offscreen-инфраструктуру. `OPACITY_MODEL_PLAN.md` §3.
- **[P3] `OpacityMask`** — вне скоупа.

## Рендер / GPU

- **[P2] `NineSliceBrush` с ВЕКТОРНЫМ источником не доведён.** Маршрут есть (`TexRectCollector` отдаёт
  `nine.Source` в тот же растровый откат), но не проверен ни глазами, ни тестом: девять кусков режутся уже из
  запечённого растра, поэтому углы и края нарезаются по пикселям бейка, а не по самой геометрии. DPI-часть этого
  пункта закрыта (масштаб устройства входит в ключ и в запрос отрисовки), осталась именно векторная нарезка.
  `TILE_BRUSH_PLAN.md`, раздел «Довести растровый откат».
- **[P2] Резкость при мелком `Viewbox`.** Запекается ВЕСЬ рисунок, а вырезка берётся уже из растра — маленький видовой
  прямоугольник мылится тем сильнее, чем он меньше. Лечение: учитывать `Viewbox` в `ImageTiling.BakeSize` и печь саму
  вырезку, а не картинку целиком. `TILE_BRUSH_PLAN.md`.
- **[P2] Обрезка прямоугольная — скруглённые контейнеры срезают углы содержимого.** `ClipToBounds` не знает про `CornerRadius`. Симптом: блик `BusyIndicator.Shimmer` доезжает до угла карточки, и угол становится прямым. Заденет любой скруглённый список/карточку/панель. Попап схлопнутой группы риббона поэтому НЕ обрезается (см. `RibbonStyleSet.auml`). Нужен попиксельный скруглённый клип в SDF-шейдерах либо маска для не-батчевого содержимого — та же инфраструктура, что у `LayerOpacity`.
- **[P2] Кисть на многоугольнике держится ТОЛЬКО на батчах.** Градиент, паттерн/шум и текстура рисуются на
  `RegularPolygon` через SDF-проходы (`GradientRectCollector`/`PatternRectCollector`/`TexRectCollector`, ветка формы
  2 в `BrushShapeDistance`). Но если батч ОТКАЖЕТ — повёрнутый или скошенный мир, переполнение буфера инстансов —
  фигура уходит на по-юнитный меш, а он такие кисти не красит: замер до появления батчей давал 82-178 закрашенных
  пикселей вместо 2752 (сплошная заливка того же гексагона). Сейчас не воспроизводится, потому что батчи забирают все
  эти случаи. У эллипса устройство отката такое же, но ЭТО НЕ ЗАМЕРЕНО. Чинить там же, где чинится по-юнитная
  заливка кистью, а не отдельным путём для многоугольника.
- **[P2] Параллельный БЕЙК.** Параллельный arrange сделан; запись юнитов/слотов в `RenderCache` — нет. Крукс = потокобезопасность бейка.

## Раскладка

- **[P1] Постройка дерева внутри ЗАМЕРА: ~9.6 мс на шаблонное поддерево.** Замер 17.08.2026 (Debug, вкладка Brushes
  открывается сразу на старте, зонд снимает фазы кадра и собственное время по типам). Пик расстановки **2997 мс** при
  1088 юнитах против **1522 мс** при 675 (разница — 72 добавленных `NumericUpDown`). Разложение:
  - инвалидация **не при чём**: 139 717 событий «пометить недействительным» — но всего **86 мс**;
  - всё время в замере, **2.7 с на 8778 вызовов**, из них по собственному времени **`ContentPresenter` — 2393 мс (87%)**,
    а сам `NumericUpDown` — **0.9 мс**;
  - и это не замер, а **постройка**: `ContentPresenter.UpdateVisualContent` строит поддерево прямо внутри
    `MeasureOverride` — **2553 мс на 265 построек**;
  - внутри постройки: **10 665 конструкторов / 766 мс** (каждый засевает слот на КАЖДОЕ зарегистрированное свойство
    типа — 615 567 контейнеров, ~58 на элемент), **85 291 запись свойства / 1706 мс собственного времени** (~20 мкс на
    запись!), навешивание стилей 319 мс, из записей около **1 с — проталкивание наследуемых значений вниз** (не
    избыточное: проверено отсечкой «ребёнок уже читает это значение» — отсеклось всего 1000 из 85 000).

  Что сделано (лежит НЕ закоммиченным): запись НЕнаследуемого свойства больше не будит детей (`AdamantiumProperty.CanInherit`
  + собственный список `inheritanceChildren` вместо подписки каждого ребёнка на `PropertyChanged` родителя) и не толкает
  значение, которое ребёнок уже читает. Обе правки верны по существу, но **эффект внутри шума**: три прогона подряд дают
  2861 / 2977 / 2897 мс, разброс ±60 мс.

  **СДЕЛАНО 18.08.2026 (НЕ закоммичено): наследование переведено на ЧТЕНИЕ.** Значение больше не проталкивается в
  каждого потомка: `AdamantiumProperty.InheritanceEpoch` — глобальный счётчик, который двигает любая явная запись
  наследуемого свойства и любая смена родителя; `ValueContainer` хранит метку эпохи, а `AdamantiumComponent.ResolveInherited`
  при чтении добывает значение у ближайшего предка и кладёт в слот `Inherited` как КЭШ. Каскад остался только там, где
  чтение бессильно: у свойства есть колбэк (`DataContext` перерешает привязки) или у элемента есть подписчики
  `PropertyChanged` (привязка/триггер, следящие за наследованным значением) — иначе обход просто идёт глубже.
  Заполнение кэша — это запись на пути чтения, чего класс до сих пор избегал, поэтому порядок записей выстроен так, что
  `Effective` (volatile) публикуется ПОСЛЕДНИМ: гонка стоит повторного разрешения, но не рвёт значение.

  Результат замером: толчков **9008 → 4002** (осталось почти только `DataContext`: 3695 колбэком, 225 по подписчикам,
  82 колбэком на `Foreground`), их цена **764 мс**, из которых **456 мс — сам `RefreshBindings`**, то есть настоящая
  работа. Пик расстановки: **2861/2977/2897 → 2755/2714/2689** (три прогона на каждую сторону), то есть
  **−190 мс, −6.6%**, и это уже вне разброса. Тесты: 1236 прошло, 0 упало.

  Почему не обещанная секунда: оценка считала цену пропорционально ЧИСЛУ толчков, а дорогие — это `DataContext`
  (перерешивание привязок), и они как раз остались. **Следующая цель поэтому другая**: 4145 вызовов `RefreshBindings`
  за постройку вкладки — смотреть, почему контекст доезжает до элемента по нескольку раз и можно ли перерешать привязки
  один раз на элемент, а не на каждый шаг каскада.

  **Размер выигрыша от наследования ЧЕРЕЗ ЧТЕНИЕ — замерены ОБЕ стороны** (18.08.2026, та же вкладка):
  проталкивание стоит **9008 записей / 1.6-1.8 с** (это 55-60% всего пика расстановки), а те же сведения, добытые
  обходом предков на чтении, — **31 293 обхода, 160 805 шагов, 22-24 мс**. По свойствам толчки делятся так:
  `Foreground` 4596, `DataContext` 4059, `FontSize` 353. Даром уходит только часть: у `DataContext` есть КОЛБЭК
  (перерешать привязки), поэтому потомкам всё равно надо сказать — но голым обходом с вызовом колбэка, без слотов,
  приведения, аллокации аргументов, разрешения приоритетов и рассылки. Оценка чистого выигрыша: **1.0-1.3 с из 2.9 с**
  (35-45%). Плата переезжает на чтение: ~0.7 мкс на непокрытое кэшем чтение, поэтому слот `Inherited` стоит оставить —
  но заполнять его ЛЕНИВО при первом чтении и сбрасывать счётчиком версии на свойство, а не проталкивать заранее.

  **СДЕЛАНО 18.08.2026 (НЕ закоммичено): 20 мкс на запись оказались НЕ записью.** Микрозамер без стенда: сама запись
  свойства стоит **2.2 мкс** (0.8 мкс, если значение не изменилось), чтение — **0.06 мкс**, запись ПО ИМЕНИ — столько же,
  сколько по ссылке. А вот **конструктор `Border` стоил 36 мкс**, `Grid` — 29. Двадцать микросекунд из приложения — это
  не одна запись, а каскад, который она запускает.

  Конструктор чинился тремя вещами, все внутри системы свойств:
  1. **слоты выделяются при первой ЗАПИСИ**, а не при рождении: элемент заводит контейнер на каждое из ~58 свойств
     своего типа и пишет от силы пять, а каждый контейнер аллоцировал `object[9]` — это 615 000 массивов на одну
     вкладку. Теперь до первой настоящей записи значение по умолчанию лежит в поле, массива нет;
  2. **одно обращение к словарю вместо четырёх** на свойство в конструкторе (было `ContainsKey` + запись + два чтения);
  3. **словарь заводится сразу нужного размера** — на 58 свойствах он до этого перестраивался шесть раз.

  Результат: конструктор **36 → 20 мкс** (`Grid` 29 → 15), чтение 0.09 → 0.06 мкс. На стенде, ПЯТЬ прогонов на каждую
  сторону (машина шумит, поэтому по медиане и минимуму): **2805 → 2338 мс по медиане (−16.6%)**, по минимуму
  2648 → 2275 (−14.1%). Тесты 1238 прошло, 0 упало.

  Мелочь, замеченная по дороге и НЕ починенная: `Thickness` — структура без `Equals`, поэтому сравнение упакованных
  значений идёт рефлексией: **385 нс** против 26 нс у `CornerRadius`, который `Equals` переопределяет (`Rect` — 169 нс).
  На пути записи сравнение зовётся дважды. Чинится добавлением `IEquatable<Thickness>`.

  **МАМОНТ НАЙДЕН 18.08.2026: кэш стилей НИКОГДА не срабатывал.** `Theme.FindStylesForComponent` кэширует
  подобранный набор стилей по ТИПУ, но условие звучало как `component.Id == null`, а `Id` зарегистрирован с умолчанием
  `String.Empty` — то есть `cacheable` был ложью ВСЕГДА, и каждый элемент заново перебирал все стили темы с
  `Selector.Match` на каждом. Замер: **450 мс на 4329 элементов, по 104 мкс** на то, что задумано как обращение к
  словарю. Правка — `string.IsNullOrEmpty`: поиск стилей **450 → 67 мс**, применение темы целиком **1049 → 314 мс**,
  пик расстановки **2207 → 1649 мс** в одном прогоне.

  Как искали (чтобы не искать так же второй раз): вкладка открывалась сразу на старте, зонд снимал фазы кадра, потом
  собственное время по типам, потом внутренности постройки. Цепочка сузилась так: расстановка → `ContentPresenter`
  строит поддерево внутри `MeasureOverride` → применение шаблонов контролов (1045 штук) → `ApplyCurrentTheme` на
  каждый элемент (4329 вызовов) → `FindStylesForComponent`. Гипотеза про квадратичный каскад прицепления по дороге
  ОТВЕРГНУТА замером: 4385 прицеплений против 4130 обходов, один к одному.

  **Итог по всем правкам (пять прогонов на каждую сторону, по медиане): 2805 → 1667 мс, −1138 мс, −40.6%.**
  Вклад по частям: кэш стилей ~550 мс, ленивые слоты + конструктор ~470 мс, наследование через чтение ~190 мс,
  карта имён свойств и `IEquatable<Thickness>` — единицы миллисекунд каждая.

  **ПРИМЕНЕНИЕ кэша разложено ЦЕЛИКОМ (18.08.2026), и сумма сходится.** Пик `ApplyFrame` **950 мс** =
  **833 мс прогрев текста** + **114 мс полный обход** (из них создание юнитов **104 мс на 942 юнита**, ~110 мкс на
  юнит) + сверка отцепленных 1 мс + очистка порядка 0. Прочие ветки применения на этом кадре не работают вовсе:
  частичная перерисовка 0, структурная 0, дельта снимка 5 мс (за всё окно), пакетов 1572, полных обходов 3.

  Внутри прогрева: перевод строк в глифы 2 мс, заливка на GPU 12 мс, **генерация MSDF 653-833 мс на 80 глифов**.
  Партия ОДНА (`batches with work: 1, biggest 80`) — пакетирование работает, `Parallel.ForEach` получает все 80 сразу,
  так что это чистая вычислительная стоимость MSDF в Debug.

  Куда двигаться: **(1)** кэш глифов НА ДИСК — MSDF детерминирован по (шрифт, глиф, размер, pixelRange, поле), второй
  запуск не платит вовсе; **(2)** прогрев АСИНХРОННО, вне критического пути; **(3)** ускорять сам
  `Glyph.GenerateDirectMSDF`; **(4)** уже внутри кэша — 110 мкс на создание юнита, это 11% фазы. Оговорка: Debug,
  в Release разрыв меньше, отдельно НЕ мерено.

  Куда двигаться: **(1)** кэш глифов НА ДИСК — MSDF детерминирован по (шрифт, глиф, размер, pixelRange, поле), так что
  второй запуск не платит вовсе (обычная практика для SDF-атласов); **(2)** прогрев АСИНХРОННО, вне критического пути —
  текст появится кадром позже, зато вход во вкладку не ждёт; **(3)** ускорять сам `Glyph.GenerateDirectMSDF`. Оговорка:
  это Debug, а MSDF — чистая арифметика, в Release разрыв будет меньше; отдельно в Release НЕ мерено.

  **Компиляция шаблонов — направление ЗАКРЫТО замером (18.08.2026).** Шаблоны УЖЕ компилируются: генератор AUML пишет
  обычный C#-метод (`Build_template_NN` в `artifacts/obj/Adamantium.UI.Themes/Generated`), который создаёт элементы и
  зовёт `SetValue` — никакого разбора дерева на лету. Внутри оставались два интерпретирующих места, оба измерены:
  поиск свойства ПО ИМЕНИ (в теме 1246 таких `SetValue`) был линейным перебором — **272 нс** против **53 нс** по карте
  имён (`AdamantiumPropertyMap.FlattenedByName`, сделано, лежит незакоммиченным), и разбор строки в значение
  (`TypeParser.Parse`, 806 мест) — **259-687 нс**. В сумме на постройку вкладки это единицы миллисекунд из 2.9 с,
  то есть **не рычаг**. Рычаги остаются прежние: 58 засеянных слотов на элемент и ~20 мкс на запись свойства.

  Куда копать дальше, по убыванию ожидаемого выигрыша: **(1)** не засевать 58 слотов на элемент — заводить
  `ValueContainer` при первой ЗАПИСИ (`values` придётся сделать конкурентным: привязки пишут с рабочих потоков);
  **(2)** 20 мкс на запись свойства — это на порядок больше, чем должно быть; подозреваемые: два
  `AdamantiumPropertyChangedEventArgs` на запись, `Equals` с упаковкой, блокировка и несколько словарных обращений;
  **(3)** наследование через ЧТЕНИЕ вместо проталкивания (модель WPF) — снимет ту самую секунду, но это уже смена
  устройства.
- **[P2] Полоса вкладок не виртуализована.** `TabControl` строит все вкладки сразу: 300 вкладок подвешивают старт. Не сводится к «включить `VirtualizingPanel`»: ширина вкладки неравномерная (а виртуализация `StackPanel` построена на равномерной ячейке), закреплённые живут в отдельном НЕвиртуализуемом хосте, и нужны ответы на «где выбранная» (прокрутка, индикатор) и «куда встаёт при перетаскивании». Начинать с `TryGetItemRect`, как для строк списка. Ловушка: контейнеры раздаёт сам `TabControl` (`ContainerOfTab` спрашивает свой генератор первым) — при виртуализации контейнера может не быть, и все, кто спрашивает «где вкладка», обязаны это пережить.
- **[P1] Прокрутка переизмеряет почти ВСЁ видимое множество каждый кадр.** Замер 14.08.2026 на вкладке Layout (60k
  плиток 24×24, 4К, полный экран): за 0.5 с — 5000–6400 реальных `Measure` и 7300–8600 `Arrange`, при том что в поле
  зрения помещается ~6400 плиток, а сдвиг на строку обязан трогать ~106. То есть за кадр перемеряется порядка всего
  окна. После правки со скелетонами (`VirtualizingPanel` больше не мигает `Visibility` у карточек, 44fb851) кадр цикла
  упал с 34–45 мс до 13–16 мс, но природа осталась прежней — просто в 4–5 раз дешевле. Копать надо в то, ЧТО именно
  инвалидирует контейнеры на каждый сдвиг: снять тот же счётчик инвалидаций по свойствам, которым нашли скелетоны
  (в `AdamantiumComponent`, ветка `AffectsMeasure`, ключ «Тип.Свойство»). Ожидаемый порядок выигрыша — ещё раз в
  несколько раз, потому что рендер при этом простаивает на 139–392 fps.

- **[P2] Нет сторожа на инвариант «устойчивая прокрутка не мигает видимостью».** Правку со скелетонами (карточка
  переезжает между слотами, а не прячется и достаётся заново) сейчас не держит ни один тест: чтобы прогнать
  `ReconcileSkeletons` вхолостую, нужен фейковый `ItemsControl` с генератором и двумя последовательными окнами слотов —
  проверять, что число смен `Visibility` = 0 при равном количестве слотов. Это ровно тот класс регрессии, что уже
  случился с ee90ab3: всё работает, просто медленно, и никто не замечает неделю. Соседние два инварианта уже
  застрахованы (`RenderCacheTests.AnIdleFramePublishesNoSnapshotEntries` / `AMoveIsStillPublished`).
- **[P2] Нет сторожа «чистый кадр обязан ВОСПРОИЗВОДИТЬСЯ, а не обходиться заново».** Ветка воспроизведения требует
  устройства, поэтому headless-тест её не берёт, а GPU-стенд (`OffscreenTestRenderer`) на каждый кадр зовёт
  `BuildFromVisualTree`, то есть чистого кадра там не бывает. Нужен либо счётчик попаданий в `ExecuteOps`, либо режим
  стенда «кадр без пересборки». Без этого регрессия вида «быстрый путь молча отключился» ловится только замером руками.
- **[P3] Intake-бюджет раскладки (дизайн).** Старый тайм-бюджет вырезан 2026-07-16: он резал measure/arrange по времени и публиковал частичное состояние в живое дерево. Настоящий должен резать по ВХОДЯЩИМ элементам, но в момент добавления стоимость add+measure+arrange неизвестна → count-cap неточен. Для не-виртуализируемых всплесков нужен time-slice по замеренному времени + staging-дерево для атомарного коммита, которого у нас нет. `LayoutManager.cs` (FrameBudget note).

## Анимация / композитор

- **[P2] `DoubleAnimation` (`RunningAnimation`) не композитится** — только keyframe `Animation`. Лечение то же: pure curve + канал.
- **[P3] Gradient-stop sweeps (shimmer) не композитятся** — таргет `GradientStop`, не `Brush`. Остаётся на loop.
- **[P3] `Compositor.RefreshBases()`** — раз в кадр на энтри (lock + property read). Пересмотреть, если композитных анимаций станет много.

## Платформенный слой / потоки

- **[P3] Хост без своего `Main`** (тест-раннер, плагин в чужом приложении — `[STAThread]` поставить некуда): нужен отдельный опт-ин `RunOnDedicatedThread()` только под Windows, не трогая дефолтный путь. Ловушка: перегрузка `UIApplication.Run(IWindow)` — окно, собранное вызывающим до `Run`, создаст HWND на чужом потоке.

## Ввод / фокус

- **[P3] Порог перетаскивания в разных единицах.** Логическая перегрузка `PlatformSettings.ExceedsDragThreshold` сравнивает логическую дельту с физическим порогом ОС: точно при 100%, чуть раньше срабатывает при 150%. Точность = тащить масштаб окна в каждый вызов. Физическая перегрузка (её использует drag-движок) точна на любом масштабе.
- **[P3] Стойка адорнера в МИРОВЫХ единицах.** `Adorner.ClipStandoff` расширяет клип вьюпорта в мировых единицах, поэтому под масштабирующим предком (DPI, ZoomBox) допуск приблизительный.
- **[P3] Фокус уходит из дерева, когда его строка уезжает за экран.** Пока строка не построена, фокус держит сам `TreeView` (клавиши работают, кольцо не рисуется) и возвращает его на строку, как только та реализована. Если фокус в этот момент был ВНЕ дерева, кольцо не встанет — воровать фокус нельзя. Обобщённого ответа «место клавиатуры в виртуализованном списке» для `ListBox` и `ItemsControl` нет, только для дерева.

## Риббон

- **[P2] Data-команды внутри группы не получают размер.** `RibbonGroup.ItemsSource` + `ItemTemplate` даёт `ContentPresenter`-контейнеры, а `RibbonGroupPanel` пишет `Ribbon.Size` в контейнер — до самой кнопки внутри шаблона он не доходит. Либо группа должна раздавать контейнеры-команды, либо размер обязан наследоваться. `docs/RIBBON_PLAN.md` §12.

## Рендер: мусор в кадре вместо честного отказа

Класс ошибок: путь, встретив то, чего не понимает, **рисует содержимое чужой памяти** вместо отказа. На
экране это атлас глифов или случайные полосы, и уводит отладку на часы. 2026-08-12 закрыто три двери —
условие батчинга теперь ОДНО (юнит спрашивает коллектор), по-юнитный путь при непонятной кисти не рисует
ничего и сообщает один раз, отрисовка с непривязанным ресурсом запрещена устройством. Осталось:

- **[P2] Кто и когда ОСВОБОЖДАЕТ слоты кучи дескрипторов — не прослежено.** Индекс слота (`GlobalHeapOffset`)
  выдаётся при создании дескриптора; если слоты переиспользуются, устаревший индекс указывает не в пустоту, а
  на **чужую живую** текстуру — именно это делало симптом плавающим. Отрисовка с непривязанным ресурсом теперь
  запрещена (`IEffectPass.ResourcesBound`), но это защита от нашей ошибки, а не от переиспользования слота.
- **[P3] Нет дескриптора-заглушки.** Непривязанный ресурс уходит в push как `uint.MaxValue`. Лучше держать
  1×1 прозрачную текстуру и общий сэмплер и подставлять их: тогда худший случай — прозрачный квадрат, а не отказ
  от отрисовки и не чужая текстура.

## Строки / локализация

- **[P2] Интерфейсные строки живут ХАРДКОДОМ на месте использования.** Ни `.resx`, ни словаря, ни менеджера строк в
  движке нет. Пока их немного и почти все в теме (`RibbonQuickAccessMenuItem` — «Add to quick access» / «Remove from
  quick access», подсказки «Back», «Collapse the ribbon»), но тема — не словарь, а строка контрола обязана быть
  переводимой. Нужен словарь с подстановкой по ключу, чтобы тема ссылалась на ключ, а не несла текст. Заводить ДО того,
  как строки расползутся по десяткам файлов.

## Дизайн / будущее (не стартовало)

- **[P3] Acrylic/Mica backdrop blur** — нужен GPU backdrop-blur-пасс. Пока оверлей = плоский scrim.
- **[P3] Pointer / multi-touch** — абстракция Pointer над mouse-only вводом.
- **[P3] Adamantium.Fonts** — пробелы стека: BiDi / line-break / fallback / hinting.
- **[P3] Vision** — Unity/Unreal-уровень IDE + UI внутри 3D. `engine-vision-positioning`.
